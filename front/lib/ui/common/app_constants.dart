/// The max width the content can ever take up on the screen
const double kdDesktopMaxContentWidth = 1200;

// The max height the homeview will take up
const double kdDesktopMaxContentHeight = 800;

/// The max width the content can ever take up on the screen
const double kdAuthMaxContentWidth = 400;

// The max height the homeview will take up
const double kdAuthMaxContentHeight = 750;

/// The max width the content can ever take up on the screen
const double kdLoginMaxContentWidth = 400;

// The max height the homeview will take up
const double kdLoginMaxContentHeight = 500;

//auth
const authBox = "LOGTOKEN";

const baseUrl = "http://127.0.0.1:8000";